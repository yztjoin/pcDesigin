const config = {
  webSite: '项目域名', //当前项目所对应的线上域名
  //上线前必须修改 默认目录是为了防止跟线上目录冲突
  folderName: '/pagesV2/User/TestWeb/' //当前项目所对应的文件夹目录名称 如果放置在根目录为空 如果有层级则如:/pagesV2/User/DriverList/
};
module.exports = config;
